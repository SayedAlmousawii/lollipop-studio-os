# Centralization — Archived Planning Docs (R0, 2026-05-19)

These two documents are the background analysis that produced the centralization roadmap. They are superseded by `context/reviews/centralization-roadmap.md`.

**Do not implement from these files.** In particular, `centralization-visual-plan.md` contains a "one DTO to all surfaces" diagram that does not reflect the approved architecture (one canonical read model, surface-specific projections).

- `centralization-inventory.md` — original gap inventory.
- `centralization-visual-plan.md` — original mermaid-based plan; contains misleading single-DTO arrows.

The active implementation contract is `context/reviews/centralization-roadmap.md`.
